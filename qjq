<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>暂无 · 渐变页面</title>
    <style>
        /* 重置默认边距，确保全屏覆盖 */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            /* 渐变背景：从淡紫到深海蓝，温和而现代 */
            background: linear-gradient(145deg, #8b9da9 0%, #2d4b6e 70%, #1a2f42 100%);
            /* 备选渐变（兼容旧浏览器） */
            background: linear-gradient(135deg, #6b7f8f, #2e4a6a, #1c3144);
            
            min-height: 100vh;            /* 至少填满视口高度 */
            display: flex;
            flex-direction: column;
            justify-content: center;      /* 垂直居中 */
            align-items: center;          /* 水平居中 */
            
            font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            
            /* 添加一点内阴影增加层次感（可选） */
            box-shadow: inset 0 0 100px rgba(0, 0, 0, 0.2);
        }

        /* 主标题 “暂无...” 样式 */
        h1 {
            color: rgba(255, 255, 255, 0.92);
            font-size: clamp(3.5rem, 18vw, 9rem);  /* 响应式，最小3.5rem，最大9rem */
            font-weight: 700;
            letter-spacing: 8px;                   /* 略微宽松 */
            text-shadow: 0 15px 30px rgba(0, 0, 0, 0.5), 4px 4px 0 rgba(0, 0, 0, 0.2);
            margin: 0 1.5rem;
            text-align: center;
            line-height: 1.2;
            animation: appear 1.2s ease-out;       /* 淡入+上浮动画 */
            
            /* 增加三个点的视觉连续性 */
            word-break: keep-all;
        }

        /* 柔和的入场动画 */
        @keyframes appear {
            0% { opacity: 0; transform: translateY(40px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* 小屏适配：稍微缩小字号，调整间距 */
        @media (max-width: 640px) {
            h1 {
                font-size: clamp(3rem, 12vw, 5rem);
                letter-spacing: 4px;
                text-shadow: 0 10px 20px rgba(0, 0, 0, 0.6);
            }
        }

        /* 极窄屏（如老手机）进一步优化 */
        @media (max-width: 420px) {
            h1 {
                font-size: 2.8rem;
                letter-spacing: 2px;
                margin: 0 1rem;
            }
        }

        /* 如果用户开启“减少动画”，则取消位移效果，保留淡入即可 */
        @media (prefers-reduced-motion: reduce) {
            h1 {
                animation: fadeOnly 1s ease-out;
            }
            @keyframes fadeOnly {
                0% { opacity: 0; }
                100% { opacity: 1; }
            }
        }

        /* 可选：在页面底部加一行极细的副文本（纯装饰，不影响主体） */
        .subtle {
            position: fixed;
            bottom: 16px;
            color: rgba(255, 255, 255, 0.3);
            font-size: 0.85rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            user-select: none;
            pointer-events: none;   /* 不干扰任何点击 */
        }
    </style>
</head>
<body>
    <!-- 主标题：暂无... 居中显示 -->
    <h1>暂无...</h1>

    <!-- 极简装饰 (可完全忽略，仅为增加一点设计感) -->
    <div class="subtle">· 占位中 ·</div>

    <!-- 无需脚本，干净简洁 -->
</body>
</html>
